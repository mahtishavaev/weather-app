import { FC } from "react";
import styled from "styled-components";

const Inner = styled.aside`
  width: 320px;
  background-color: #1e213a;
`;

export const Search: FC = () => {
  return <Inner></Inner>;
};
